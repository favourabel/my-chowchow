# TODO - Vendorpage UI/Responsiveness

- [x] Refactor hero section in `src/pages/Vendorpage.jsx` to use responsive typography and remove fixed left margins.
- [x] Make all images fluid (use `w-full max-w-* h-auto`) and remove fixed `h-[...]` where it causes overflow.
- [ ] Convert the repeated story/post cards into a responsive grid layout.

- [x] Refactor FAQ + download/CTA sections for mobile-first spacing and scalable font sizes.

- [ ] Refactor footer to remove large `ml-[...]` offsets and use wrapping/grid columns.
- [ ] Quick cleanup: add missing `alt` attributes and remove obvious invalid layout issues nearby.
- [ ] Run dev/build to ensure there are no JSX/Tailwind class typos.




